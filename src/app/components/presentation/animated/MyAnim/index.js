/**
 * MyAnim should be a container, a paren, who has CSS and CSS3 animation configurationa
 * it has multiple type of animations, which can be fully customizable...
 * just wrap a children inside and it will inherit the animations configurations
 */
