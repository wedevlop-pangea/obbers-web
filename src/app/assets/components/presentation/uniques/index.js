/**
 * Organisms use Molecules, Molecules use Atoms. That pattern makes one sort of
 * components depend on others. Low hierarchy components have 0 dependencies, higher
 * hierarchy components depend much more on low hierarchy components.
 * Uniques does not depend on any other components at all, everything is within them.
 **/
